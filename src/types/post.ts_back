export type Post = {
  id: string;
  title: string;
  slug: string;
  description: string;
  body: string;
  tags?: string;
  imageUrl?: string;
  status: number;
  createdAt: string;
  updatedAt: string;
  user?: string;
  userId: string;
};

export type Tags = {
  id: string;
  title: string;
  rate: number;
};
